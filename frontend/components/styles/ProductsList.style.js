import styled from 'styled-components';

const ProductsContainer = styled.div`
  max-width: var(--maxWidth);
  margin: 0 auto;
`;

const PaginationRow = styled.div`
  margin-top: 3rem;
  display: flex;
  justify-content: center;
`;

export { ProductsContainer, PaginationRow };
